import 'package:flutter/material.dart';
import '../widget/splach_body.dart';
class SplashScreen extends StatelessWidget {
  const SplashScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: SplachBody(),
    );
  }
}
